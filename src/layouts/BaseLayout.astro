---
import "../styles/nuvya.css";
import "../styles/site.css";
import SEO from "../components/SEO.astro";
import Nav from "../components/Nav.astro";
import Footer from "../components/Footer.astro";

interface Props {
  title: string;
  description: string;
  path?: string;
  image?: string;
  type?: "website" | "article";
  structuredData?: Record<string, unknown>;
}

const { title, description, path = Astro.url.pathname, image, type, structuredData } = Astro.props;
---
<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <SEO title={title} description={description} path={path} image={image} type={type} structuredData={structuredData} />

  <link rel="icon" href="https://nuvyavc-favicon.chinkukoolwal.workers.dev" type="image/jpeg" sizes="768x768" />
  <link rel="apple-touch-icon" href="https://nuvyavc-favicon.chinkukoolwal.workers.dev" />
  <link rel="manifest" href="/manifest.webmanifest" />

  <link rel="preload" href="/assets/fonts/geist-300.woff2" as="font" type="font/woff2" crossorigin />
  <link rel="preload" href="/assets/fonts/geist-500.woff2" as="font" type="font/woff2" crossorigin />
  <link rel="preload" href="https://nuvyavc-favicon.chinkukoolwal.workers.dev" as="image" />
</head>
<body class="nuvya">
  <div class="nv-atmosphere" aria-hidden="true">
    <div class="nv-atmosphere__glow"></div>
    <div class="nv-atmosphere__grain"></div>
  </div>

  <Nav />

  <main>
    <slot />
  </main>

  <Footer />

  <script src="../scripts/motion.ts"></script>
</body>
</html>
