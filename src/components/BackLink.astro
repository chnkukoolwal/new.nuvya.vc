---
interface Props {
  href: string;
  label: string;
}
const { href, label } = Astro.props;
---
<a class="nv-back nv-reveal" href={href}>
  <span class="nv-back__icon" aria-hidden="true">&larr;</span>
  <span>{label}</span>
</a>
